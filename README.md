# Tsubaiso Intelligence Skill

Tsubaiso Intelligence（サービス）の**配布スキルパッケージ（プラグイン）**です。PSA/IMA（Salesforce）業務を Claude が支援するためのスキル群を収録します。

> 用語: 「Tsubaiso Intelligence」はサービス総称、「Tsubaiso Intelligence Skill」は本配布物（プラグイン）の名称です（GLOSSARY 準拠）。

## 収録スキル

| スキル | 内容 | 層 |
|---|---|---|
| `skills/ti-core` | 全スキル共通の土台。発火点で読む安全ゲート・出力規律・現状仕様の往復・フィードバック集約 | 層1（横断土台） |
| `skills/ti-reference` | PSA/IMA データ参照の知識層。意味・ルール・定番 SOQL レシピを Tsubaiso Atlas MCP から取得し、構造（型・ラベル・関係）は利用組織の describe で補完。書込前構造ゲート（write-index）は Atlas MCP の更新系ツールから構造情報を取得して判定する | 層2（能力） |
| `skills/ti-metadata` | PSA/IMA と整合したメタデータ＝カスタマイズ（カスタム項目・権限セット・入力規則・承認プロセス・FlexiPage・Flow・LWC・VF・CMDT 等）の差分方式設定 | 層2（能力） |
| `skills/ti-report` | 帳票（見積・請求・注文・納品・検収 等）のレイアウト・項目カスタマイズ | 層2（能力） |
| `skills/ti-update` | 稼働後のデータ更新全般（トランザクション作成・更新＋マスタ日常メンテの入力支援・承認前チェックの承認支援） | 層2（能力） |
| `skills/ti-data-load` | 初期一括ロード・移行投入（別環境からの一括移行・期首残高・大量デモデータ）の実行 | 層2（能力） |
| `skills/ti-spec-view` | 顧客が org へ加えたカスタマイズ仕様の可視化・ブラックボックス化の解消 | 層2（能力） |
| `skills/ti-local-automation` | 標準 MCP ツールで届かない操作（大容量・バイナリのファイル添付＋公開リンク発行、Bulk/Composite、独自 Apex REST）を、本人権限内でローカルスクリプトから行う機構 | 層2（能力） |
| `skills/ti-lifecycle` | カスタマイズを「どの実装形態で・どのフェーズで作るか」の判断（開発運用ライフサイクル） | 層3（判断） |
| `skills/ti-rollout` | TIを顧客環境で使い始めるための接続・環境セットアップの進行（MCP/コネクタ結線・接続用アプリ作成・接続キー取得・初期セットアップ・利用者の本人接続） | 層3（進行） |
| `skills/ti-onboarding` | 本番導入プロジェクトの工程進行（計画〜設計〜制作〜構築〜データ移行〜教育〜運用テスト） | 層3（進行） |
| `skills/ti-poc` | 契約前の PoC・商談・社内検証でのトライアル体験（4体験を1本のナラティブで束ねる） | 層3（進行） |

## 使い方

1. 標準（ホステッド）Salesforce MCP を接続する（ツバイソヘルプ記事 ID 58638669783065）
2. Tsubaiso Atlas MCP を接続する（意味・ルール・定番レシピの配信元）
3. `skills/ti-reference` のスキルを読み込む
4. PSA/IMA のデータについて尋ねる（オブジェクト探索・SOQL 作成・結果のリンク化）

## 位置づけ（置き場分離）

- 本リポジトリ＝**配布スキル**（低 IP の雛形・スクリプト）。
- 意味・ルール・定番レシピの本体は Tsubaiso Atlas MCP サーバーが実行時に配信し、本パッケージには同梱しない。
- 書込前構造ゲートも Atlas MCP の更新系ツールを実行時に参照する。**意味定義・構造スナップショットの同梱はゼロ**で、本パッケージにローカルの意味定義ファイルは残っていない。

## フェーズ

読み取りの意味・ルール・レシピも、書込前構造ゲートが使う構造情報も、Tsubaiso Atlas MCP サーバーが実行時に配信する（同梱ゼロ）。

本パッケージは低 IP のみで構成されるため、**public マーケットプレイスとして配布**する。マーケットプレイスを一度登録すれば以後の改訂・スキル追加を受け取れ、登録にあたって招待・認証・追加課金は要らない。**利用可否を決めるのは配布物へのアクセスではなく、Tsubaiso Atlas MCP サーバーからの意味定義配信**で、契約の終了にともなって配信を停止する。
